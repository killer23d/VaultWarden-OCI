name: PR10 final systemd validation

on:
  push:
    branches:
      - agent/pr10-platform-simplification

permissions:
  contents: read

jobs:
  systemd-verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
        with:
          ref: agent/pr10-platform-simplification
          fetch-depth: 0
      - name: Verify Compose and systemd units
        run: |
          docker compose --env-file .env.example -f docker-compose.yml.example config --quiet
          docker compose --env-file .env.example -f docker-compose.yml.example -f docker-compose.override.dev.yml.example config --quiet
          systemd-analyze verify systemd/*.service systemd/*.timer
