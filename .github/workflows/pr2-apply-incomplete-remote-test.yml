name: PR2 apply incomplete remote test
on:
  push:
    branches: [fix/authenticated-backup-cohort]
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/authenticated-backup-cohort
          fetch-depth: 0
      - name: Apply and validate test edit
        shell: bash
        run: |
          set -euo pipefail
          python3 .github/pr2-incomplete-remote-test.py
          bash -n tests/suites/data-protection/case-restore-recovery.bash
          shellcheck -x --severity=warning tests/suites/data-protection/case-restore-recovery.bash
          git diff --check
      - name: Commit test and remove helpers
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git rm .github/workflows/pr2-apply-incomplete-remote-test.yml .github/pr2-incomplete-remote-test.py
          git add tests/suites/data-protection/case-restore-recovery.bash
          git commit -m 'tests: reject incomplete remote cohort selection'
          git push origin HEAD:fix/authenticated-backup-cohort
