name: Doc Drift Check
on:
  pull_request:
    paths:
      - '.github/workflows/**'
      - 'AGENTS.md'
      - 'caddy/**'
      - 'crowdsec/**'
      - 'docs/**'
      - 'README.md'
      - 'RUNBOOK.md'
      - 'utilities/**'
      - 'lib/**'
      - '*.sh'
      - 'Makefile'
      - 'VERSION'
      - 'systemd/**'
      - 'tests/**'
      - 'secrets-schema.yaml'
      - '.env.example'
      - 'docker-compose.yml.example'
      - 'docker-compose.override.dev.yml.example'
      - '.gitignore'

# Global permissions
permissions:
  contents: read

jobs:
  compose-config:
    name: Docker Compose config
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5

      - name: Validate Docker Compose examples
        run: |
          docker compose version
          docker compose \
            --env-file .env.example \
            -f docker-compose.yml.example \
            config --quiet
          docker compose \
            --env-file .env.example \
            -f docker-compose.yml.example \
            -f docker-compose.override.dev.yml.example \
            config --quiet

  doc-drift:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5

      # yq is required by lib/schema.sh (used by secrets-rotate.sh --help and
      # write-command-reference.sh). Installing it here ensures the generated
      # COMMAND-REFERENCE.md is identical to what was committed (which was
      # produced on a machine that had yq installed).
      #
      # NOTE: 'apt-get install yq' installs python-yq (a jq wrapper), NOT
      # the Mike Farah yq v4 binary required by production setup and
      # lib/schema.sh. Keep this pin in sync with utilities/setup-system.sh.
      - name: Install yq
        run: |
          # Pinned to v4.53.3; mutable release discovery is intentionally not used.
          # To upgrade: update YQ_VERSION and YQ_SHA256 together.
          # SHA-256 source: https://github.com/mikefarah/yq/releases/tag/v4.53.3
          # (asset: yq_linux_amd64, digest field)
          YQ_VERSION="v4.53.3"
          YQ_SHA256="fa52a4e758c63d38299163fbdd1edfb4c4963247918bf9c1c5d31d84789eded4"
          curl -fsSL \
            "https://github.com/mikefarah/yq/releases/download/${YQ_VERSION}/yq_linux_amd64" \
            -o /tmp/yq
          echo "${YQ_SHA256}  /tmp/yq" | sha256sum --check --status
          sudo install -m 755 /tmp/yq /usr/local/bin/yq
          yq --version

      - name: Check for known stale terms
        run: |
          STALE_TERMS=(
            "simple_key_resilience.sh"
            "--quick-verify"
            "cs-cloudflare-bouncer"
            "make logs FOLLOW=true"
            "--puid"
            "--pgid"
            "simple_verify_age_key"
            "utilities/generate-command-ref.sh"
            "VWOCI-PRR-PATCH"
          )
          FAIL=0
          for term in "${STALE_TERMS[@]}"; do
            matches=$(grep -rn -- "$term" docs/ README.md RUNBOOK.md 2>/dev/null || true)
            if [[ -n "$matches" ]]; then
              echo "STALE TERM FOUND: $term"
              echo "$matches"
              FAIL=1
            fi
          done
          if grep -Fn -- 'init-permissions' utilities/pre-production-drill.sh docker-compose.yml.example; then
            echo "STALE COMPOSE SERVICE FOUND: init-permissions"
            FAIL=1
          fi
          [[ $FAIL -eq 0 ]] || exit 1

      - name: Verify Makefile targets referenced in docs exist
        run: |
          FAIL=0
          while IFS= read -r target; do
            if ! grep -q "^${target}:" Makefile; then
              echo "MISSING MAKEFILE TARGET referenced in docs: make $target"
              FAIL=1
            fi
          done < <(grep -roh 'make [a-z_-]\+' docs/ README.md RUNBOOK.md | sed 's/make //' | sort -u)
          [[ $FAIL -eq 0 ]] || exit 1

      - name: Verify script flags referenced in docs exist
        run: |
          check_flag() {
            local script="$1" flag="$2"
            if ! grep -q -- "$flag" "$script" 2>/dev/null; then
              echo "FLAG NOT FOUND: $flag in $script (referenced in docs)"
              return 1
            fi
          }
          FAIL=0
          check_flag utilities/backup-run.sh "--full-verification" || FAIL=1
          check_flag utilities/maintenance-update.sh "--images"     || FAIL=1
          check_flag utilities/maintenance-update.sh "--system"     || FAIL=1
          check_flag utilities/maintenance-update.sh "--all"        || FAIL=1
          check_flag setup.sh "--force"                             || FAIL=1
          check_flag utilities/uninstall-vaultwarden.sh "--dry-run" || FAIL=1
          [[ $FAIL -eq 0 ]] || exit 1

      - name: Check pinned production version contract
        run: |
          FAIL=0
          if grep -RFn -- '--use-latest' setup.sh utilities/setup-system.sh utilities/setup-env.sh utilities/setup-crowdsec.sh; then
            echo "UNPINNED: mutable --use-latest production setup mode is forbidden"
            FAIL=1
          fi
          if grep -RFn --include='*.sh' -- '/releases/latest' setup.sh utilities/ lib/ 2>/dev/null; then
            echo "UNPINNED: production shell code must not resolve mutable GitHub latest releases"
            FAIL=1
          fi
          if grep -RFn --include='*.sh' -- '@latest' setup.sh utilities/ lib/ 2>/dev/null; then
            echo "UNPINNED: mutable @latest reference found in production shell code"
            FAIL=1
          fi
          if ! grep -Fq 'SOPS_DEFAULT_VERSION="v3.13.2"' utilities/setup-system.sh; then
            echo "UNPINNED: setup-system.sh must define the pinned SOPS default"
            FAIL=1
          fi
          if grep -Eq '^(VAULTWARDEN_VERSION|CADDY_VERSION|POSTFIX_VERSION|BUSYBOX_VERSION|CROWDSEC_VERSION|CF_WORKER_BOUNCER_VERSION|FIREWALL_BOUNCER_VERSION)=latest$' .env.example; then
            echo "UNPINNED: production component version in .env.example"
            FAIL=1
          fi
          if grep -En '^[[:space:]]*--with[[:space:]]+github.com/[^[:space:]@]+([[:space:]\\]|$)' caddy/Dockerfile; then
            echo "UNPINNED: direct xcaddy module reference lacks an @version"
            FAIL=1
          fi
          if grep -En '^[[:space:]]*--with[[:space:]]+github.com/.*@(latest|main|master|HEAD)([[:space:]\\]|$)' caddy/Dockerfile; then
            echo "UNPINNED: direct xcaddy module uses a mutable ref"
            FAIL=1
          fi
          [[ $FAIL -eq 0 ]] || exit 1

      - name: Check Noble-only production host assumptions
        run: |
          FAIL=0
          for term in 'stat -f' 'shasum' 'dnf' 'yum'; do
            matches=$(grep -RFn --include='*.sh' -- "$term" setup.sh startup.sh maintenance.sh backup.sh restore.sh recover.sh lib/ utilities/ 2>/dev/null || true)
            if [[ -n "$matches" ]]; then
              echo "UNSUPPORTED HOST FALLBACK FOUND: $term"
              echo "$matches"
              FAIL=1
            fi
          done
          [[ $FAIL -eq 0 ]] || exit 1

      - name: Systemd unit hardening check
        run: |
          REQUIRED_DIRECTIVES=(PrivateTmp ProtectSystem ProtectHome NoNewPrivileges)
          FAIL=0
          for unit in systemd/vaultwarden-*.service; do
            [[ "$unit" == *template* ]] && continue
            for directive in "${REQUIRED_DIRECTIVES[@]}"; do
              [[ "$unit" == *iptables* ]] && [[ "$directive" == "NoNewPrivileges" ]] && continue
              if ! grep -q "^${directive}=" "$unit"; then
                echo "MISSING $directive in $unit"
                FAIL=1
              fi
            done
          done
          [[ $FAIL -eq 0 ]] && echo "All units pass hardening check." || exit 1

      - name: Verify COMMAND-REFERENCE.md is up to date
        env:
          # Suppress the docker.sh Compose-project-label diagnostic that
          # scripts emit to stderr when DOCKER_PROJECT_LABEL is unset.
          # Setting it to a fixed value makes --help output deterministic.
          DOCKER_PROJECT_LABEL: ci
        run: |
          cp docs/COMMAND-REFERENCE.md /tmp/COMMAND-REFERENCE.committed.md
          bash utilities/write-command-reference.sh
          if ! diff -q docs/COMMAND-REFERENCE.md /tmp/COMMAND-REFERENCE.committed.md > /dev/null 2>&1; then
            echo "COMMAND-REFERENCE.md is stale. Run: make docs"
            diff docs/COMMAND-REFERENCE.md /tmp/COMMAND-REFERENCE.committed.md || true
            exit 1
          fi
          echo "COMMAND-REFERENCE.md is up to date."

  shellcheck:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: Check pull request diff
        run: |
          git fetch --no-tags origin \
            "${{ github.event.pull_request.base.sha }}:refs/remotes/origin/${{ github.event.pull_request.base.ref }}"
          git diff --check "origin/${{ github.event.pull_request.base.ref }}...HEAD"

      - name: Validate Bash syntax
        run: |
          find . \
            -path './.git' -prune -o \
            -type f \( -name '*.sh' -o -name '*.bash' \) -print0 \
            | xargs -0 -n 1 bash -n

      - name: Run Strict Shellcheck
        run: |
          # -x: follow source statements to avoid SC1091 false positives.
          # --severity=warning: fail on warning or error; info/style are logged only.
          find . -type f \( -name '*.sh' -o -name '*.bash' \) -print0 \
            | xargs -0 shellcheck -x --severity=warning

  functional-tests:
    name: Functional tests (${{ matrix.suite }})
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        suite:
          - foundation
          - security
          - operations
          - data-protection
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5

      - name: Install test dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y age apache2-utils 7zip python3-argon2 python3-bcrypt python3-yaml
          curl -fsSL https://github.com/mikefarah/yq/releases/download/v4.53.3/yq_linux_amd64 -o /tmp/yq
          echo "fa52a4e758c63d38299163fbdd1edfb4c4963247918bf9c1c5d31d84789eded4  /tmp/yq" | sha256sum --check --status
          sudo install -m 755 /tmp/yq /usr/local/bin/yq
          curl -fsSL https://github.com/getsops/sops/releases/download/v3.13.2/sops-v3.13.2.linux.amd64 -o /tmp/sops
          echo "154dfe4cd70554bdd82b98e4cd4acf191d43d01ead6f00a73477aa44c4ac42ef  /tmp/sops" | sha256sum --check --status
          sudo install -m 755 /tmp/sops /usr/local/bin/sops

      - name: Run ${{ matrix.suite }} suite
        shell: bash
        run: |
          mkdir -p test-results
          log="test-results/${{ matrix.suite }}.log"
          set +e
          ./tests/run-tests.sh "${{ matrix.suite }}" >"$log" 2>&1
          rc=$?
          set -e
          cat "$log"
          if (( rc != 0 )); then
            exit "$rc"
          fi
          if [[ "${{ matrix.suite }}" == "security" ]]; then
            grep -Fq \
              'PASS direct automatic setup PTY no-leak regression' \
              "$log" || {
                echo "ERROR: mandatory direct-auto PTY no-leak regression did not execute" >&2
                exit 1
              }
          fi

      - name: Upload ${{ matrix.suite }} suite log
        if: always()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: test-log-${{ matrix.suite }}
          path: test-results/${{ matrix.suite }}.log
          if-no-files-found: error
          retention-days: 7
